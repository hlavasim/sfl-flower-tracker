{
  "name": "sfl-flower-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cp dist/index.html . && npx vercel --yes --prod"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
