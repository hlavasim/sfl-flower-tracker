# Handoff — 2026-07-30

Continues `handoff-2026-07-29.md`, whose §2 and §4 diagnoses were both wrong (corrected in a
status block at the top of that file). Everything below "Shipped" is live on production and
pinned by a test that was shown to fail when the fix is reverted. **244/244.**

## Conventions this repo expects

Unchanged from the previous handoff, plus four earned the hard way today:

- **`npm test | grep` exits 0 whether or not anything failed.** An `&& git push` chain after it
  will happily ship red — it did once today. Gate on the failure COUNT:
  `FAILED=$(npm test 2>&1 | grep -c "^not ok"); if [ "$FAILED" != "0" ]; then exit 1; fi`
- **Verify that a revert actually applied** before believing a red/green result. A scripted edit
  that silently no-ops (wrong line endings, an emoji stored as `\uXXXX`, bash eating quotes)
  leaves the suite green and reads as proof. Two near-misses today.
- **A/B on the live farm must run in ONE pass.** Two runs of identical code disagreed because a
  7-day shrine expired between them (Badger = trees+stone, Mole = iron/gold/crimstone, both
  speed 0.75). That artefact produced a 44-row "regression" table that did not exist, and
  earlier a phantom 0.5–0.8x engine divergence. Compare both sides inside one
  `page.evaluate`, not across two HTTP requests.
- **`powerState`, `ROADMAP_MINING_CATS` and every other top-level `const`/`let` in flowers.html
  are NOT on `window`.** Only `function` declarations are. Three diagnostics today reported
  nonsense (`46 mismatches`, `owned: 0`, `timed out`) because a probe read
  `window.powerState`. Reach the values through the global functions that close over them, or
  fetch them from the API.
- Measure UI fixes with `playwright-core` driving installed Edge
  (`chromium.launch({ channel: "msedge" })`) — no browser download, and it can override page
  functions at runtime for an A/B. Read `textContent`, not `innerText`: the latter is
  layout-dependent and returns "" for a hidden element.

## Shipped today

- **Wishlist**: bud ROI resolved server-side via `core/engine/buds.mjs`; `budboost:<filter>`
  category rows using the game's own Marketplace → Bud NFTs → Boost taxonomy (19 entries,
  cheapest listing wins); `—` vs `?` split; the ✕ and priority dots worked for no item with an
  apostrophe in its name (`escJsAttr`).
- **Valuation**: `calcBoostValue` clamps nothing now (6 of 277 items move, none owned — they
  become warnings, e.g. Acre Farm −0.74/day); `free_feed` is no longer inert; per-harvest coin
  drops are priced (`coin_chance`, Money Tree); `-50% Oil Regeneration Time` was parsed as
  HALVING the oil (Dev Wrench −0.47 → +11.42).
- **Permanent value is annualised.** `calcBoostValue` asks for `seasonBasis: "annual"`, which
  runs plot selection once per season and averages. Only Grape/Olive/Rice/Wheat are year-round;
  14 products are 2/4 seasons and 13 are 1/4, and the old in-season gate credited them at 100%
  or 0% — up to 4x out. In-season basis untouched for "what am I earning now".
- **Ascension ranks migrated to core** (`core/engine/skill-ranks.mjs`), served by
  `section=power` as `skillRanks`; `section=power` accepts POSTed snapshots and reports
  `valueBasis: "measured"`.
- **Roadmap**: "All skill upgrades by ROI" panel — L1 + L2 + L3 as a buyable ladder, per-point
  column, cumulative points/shards, affordability line, click-to-expand. Reads served data
  only. "Best rebuild order" removed (it froze the page).
- **Nodes**: Oil Reserve and Flower Bed added to the acquisition table (both were absent; oil
  had been excluded on purpose and that was wrong). `unpriced` flag separates "earns nothing"
  from "cannot be priced" — no flower has a p2p price at all.
- **Ascension coin stock includes treasures you could sell** (~238k coins, more than the cash
  on hand), reusing the treasury's `TREASURE_SELL_PRICES` and boosts.

## Queue, in order

1. **Merge vs buy is inconsistent with the NODES page.** Roadmap prefers merging a gold node to
   T2 then buying; NODES says a new node is 2x better (half the ROI). `nodeAcq` computes buy and
   expand in `core/sections/ascension.mjs`; MERGE has its own path via `MERGE_COSTS` in
   flowers.html. Two paths — the exact shape that produced three separate bugs today. Measure
   both sides in one pass, then unify.
2. **Composters, and the whole fertiliser model.** `COMPOST_RECIPES` exists and `item-value.mjs`
   prices the outputs, but **what fertiliser DOES is modelled nowhere** — no rule treats Sprout
   Mix as a crop yield boost or Rapid Root as a growth-time cut. So "is a composter worth its
   yield bonus" cannot be answered: the bonus side is missing. Consequence: the entire Compost
   skill tree values at 0, including `Sprout Surge` ("Sprout Mix on all plots"). Needs the
   effect table pulled from the GAME SOURCE (as `SKILL_POINTS_PER_TIER` and the node prices
   were), then a `fertiliser` effect type, then the composter as a PERIODIC action (it has a
   cycle and inputs), then the verdict.
3. **Buy-path table shown by default**, plus a FLOWER price column.
4. **Periodic / daily section** — a second section beside the upgrade one, ranked by value per
   minute of play rather than ROI. Start with the three already computed: unsold treasures,
   expired shrines (one expired mid-session and nothing surfaced it), and restock queues
   (`restockQueues.daysUntilEmpty`). Then deliveries (and to whom), digging, weather-item
   renewal, idle kitchens, fishing daily limit.
5. **Skills and ascensions into the buy path.** Owned skills can be taken now; unowned ones must
   carry the cost of new skill points; ascension shards need a price — the owner's proposal is
   1 shard = a Gold Pickaxe's tool cost, which is a DERIVATION, not a market price, and must be
   written down as an assumption.

## Facts worth not re-deriving

- Skill tier gates are per tree, from `SKILL_POINTS_PER_TIER` in the game's
  `events/landExpansion/choseSkill.ts`: Animals 4/8, Crops & Mining & Compost & Aging 3/7, the
  rest 2/5. **Points spent on tier-3 skills never count towards a gate**
  (`getUnlockedTierForTree` skips them).
- `bumpkin.skills` stores the current LEVEL as a number, not a presence flag.
- Node prices from `RESOURCE_NODE_PRICES` (`buyResource.ts`): Crop Plot 3/2, Tree 4/3, Stone 4/3,
  Fruit Patch 5/5, Iron 7/5, Gold 10/6, Crimstone 20/20, Flower Bed 30/25, Oil Reserve 40/20
  (desert-gated), Lava Pit 40/40 (volcano-gated). Escalation reads
  `farmActivity["<Node> Bought"]`, not the owned count.
- `farm.flowers` is a CONTAINER (`{flowerBeds, discovered}`), not a list of beds — counting it
  directly gives 2 where the farm has 4.
- 87 of the 153 skills have no Level 2/3 at all (Acre Farm and Hectare Farm among them).
- `roadmapEffFactor` can exceed 1 (gold 1.50 — the `ratio > 1.5` clamp in
  `roadmapComputeEfficiency` — trees 0.54). It is not a discount, it is a measured ratio.
- No flower has a price in the sfl.world p2p feed; only Sunflower and Cauliflower, which are
  crops.
- Frugal Miner and its kin discount the pickaxe's COIN price only, not the wood/stone/iron it is
  crafted from. `power-helpers` calls that variable `coinDiscount` for a reason; pricing the
  whole tool bill ran ~30x hot.

---

## Status update — same day, later (through commit 6b7d818)

Everything below is deployed to production and verified there, not just locally.

### The unification is done: NODES computes nothing

The demand was "všechno přece má vycházet z jednoho místa". `nodeAcq` (section=ascension) is now
the only place node economics exist, and the NODES page renders it. Deleted from `flowers.html`:
`calcMarginalForProduct`, `getMarginalNetSfl`, `mergeEff`, `getNodeTiers`, `restockCostPerUnit`,
`PRODUCT_LISTS`, `NODE_FIXED_PRODUCTS`, `getRealRoi`, both `MERGE_COSTS` loops, and the page's own
dig-session efficiency pass.

The numbers moved, as agreed, and three of the moves are fixes:

- **Efficiency was applied twice.** The marginal already carried the ratio, then the table did
  `REAL = theo × eff` on top of it. Every "real" income and ROI on that page was scaled by eff².
- **Purchase prices escalated off the owned count.** The game reads
  `farmActivity["<Node> Bought"]`.
- **Buy Gold / Buy Crimstone were listed UNPROFITABLE** there while the roadmap and the ascension
  plan called them profitable — the reported bug. Buy Gold now reads +0.7114/day, 196 d ROI, and
  both pages print identical per-node figures.

Two more defects the rendered output exposed: CURRENT NODES printed `0.00` FLOWER for oil,
obsidian and flower beds (units/day now, with ⚠ where the resource sells but has no p2p price),
and the greedy plan's 50-step safety cap truncated silently (it says so now).

Served for this, each pinned: per-node `tiers` for all ten node types (not just the mergeable
four), `eff: {byCat, meta}`, `cat`, `buyRule`, merge `effRatio`, merge `coinSfl`, and `buy` at
depth 8. Two real bugs found while serving them: **merge `matSfl` was missing its coin half**
entirely, and the expand-vs-buy verdict was still being re-derived in the panel's render.

`NODE_PRICES`, `BASE_NODE_COUNTS`, `MERGE_COSTS` and `countNodeTiers` are now unused in
`flowers.html`. Left in place and marked with a comment, not deleted.

### Weather items: the reader was looking in the wrong place

"A wather items normálně mám, takže tam máš nějakou chybu" — correct. The game keeps placed
collectibles in **four** maps (`getCollectiblesAcrossLocations`): `collectibles`,
`home.collectibles`, `interior.ground.collectibles`, `interior.level_one.collectibles`. Both
readers checked only the first two, and on a current save **`home.collectibles` is empty** — the
house layout change moved everything to `interior.ground.collectibles`. A farm with all four items
placed reported all four missing. The dashboard card went from "4/4 MISSING" to "ALL 4 ACTIVE".

Three further things were wrong once the items could be found:

- placement requires `coordinates` (no coordinates = in the chest, per `placeCollectible.test.ts`);
- it requires `readyAt <= now` (still building protects nothing);
- the dashboard asked "is it placed?" and showed a green ✓ for a **spent** item — the trap in
  "vyprší ale zůstanou postavené tak na to pozor". `markWeatherCollectibleUsed` sets
  `used: true` and leaves it placed. Three states now, and the card names what is uncovered.

Renewal cost is no longer "unknown": `WEATHER_SHOP_ITEM_COSTS × getMultiplier(island)` (volcano
×2.5, desert ×2), which `renewWeatherCollectible.ts` charges with craft-time discounts
deliberately not applied. Priced through the same `_shrineIngredientCostSfl` the shrine rows use.
Coins stay in coins — this app treats them as free above 10k.

## Still open

1. **Skills and ascensions into the BUY PATH** (item 5 above, unchanged). They are already unified
   in one ROI-sorted table in the Skills tab (`roadmapAllUpgradesHtml`, L1 by skill points and
   L2/L3 by Ascension Shards) — what is missing is folding them into `sim.ranked`.
   `roadmapSimulate` documents why they are excluded: it is a FLOWER-reinvestment order and skills
   cost points. Two things must be decided before writing code: free points make a skill **cost
   zero and therefore first**, not a competitor in the queue; and a shard needs a price (the
   owner's 1 shard = Gold Pickaxe tool cost is a derivation, not a market price).
2. **`roadmapNodeCandidates` is a fourth copy of the node model** — its own escalation
   (`tiers.effective − BASE_NODE_COUNTS`) and its own merge margins, so the buy path's node rows
   are not the ones the NODES page now shows. Measured on farm 155498: the two escalation rules
   give **identical** sunstone prices for all ten nodes, because `bought` happens to equal
   `effective − base` on that farm. So there is no visible discrepancy today, only the wrong rule
   — it diverges as soon as expansions grant nodes at a rate the base table does not predict. The
   margin bases still differ and that is NOT unified.
3. **Digging** — `TREASURE_SELL_PRICES` (20 items) exists; the dig-rate model does not.
4. **Compost skills value 0** for two distinct reasons, both still true: `Efficient Bin`
   ("+5 Sprout Mix") is more fertiliser per batch, and `Sprout Surge` is a yield effect conditional
   on having the mix.
5. **The roadmap renders twice** ("Building roadmap" flashes). Unmeasured. Suspected: the first
   pass runs with no history (obsidian/den 0 vs 4.587).

## Facts worth not re-deriving (additions)

- Placed collectibles live in FOUR maps; `farm.home.collectibles` is EMPTY on current saves.
  `farm.interior.ground.collectibles` is where the house's ground floor is now.
- Weather protection is single-use and event-driven, never timed. `used: true`, still placed.
- Weather Shop: Tornado Pinwheel & Mangrove `{Wood 30, Leather 5}` 100c, Thermal Stone
  `{Stone 5, Wool 5}` 100c, Protective Pesticide `{Wood 30, Feather 30}` 100c — all × island
  multiplier (volcano 2.5, desert 2). The multiplier is applied without rounding, so a volcano
  renewal genuinely asks for 12.5 Leather.
- `grep -c` exits 1 on a zero count, so `FAILED=$(npm test | grep -c "^not ok") && ...` aborts the
  chain when the suite is GREEN. Use `|| true`.

---

## Status update 2 — "vyřeš to všechno" (through commit 684b8cf)

All five items from the previous list were taken on. Everything below is deployed and verified on
production against farm 155498, except where the last section says otherwise.

1. **Skills and ascension ranks in the buy path.** A skill point IS bought — with the FLOWER of
   the food you cook for the XP — and `skillCostInfo` already derived that rate, so the old
   exclusion ("they cost points, not FLOWER") was only half right. Points you already HOLD make a
   skill cost zero and put it at the front; points you don't cost `points × sflPerPoint`. Free
   points cover a skill **partially** — the first version asked "does it fit entirely?", so the
   farm's single spare point could not touch Hectare Farm (3pt, best per point at +1.60/pt) and
   went to the best 1pt skill instead (+0.03/pt), 50x worse. Ranks (L2/L3) add Ascension Shards,
   priced by the owner's derivation: 1 shard = a Gold Pickaxe's MATERIALS (≈1.13 FLOWER), coins
   excluded because the roadmap treats coins as free. `shardNote` travels on every row that uses
   it. 31 rows reach `sim.ranked`.
2. **Ascension EXPANSIONS in the buy path** (asked for separately). Exactly ONE row: the next step
   that earns something, carrying the cost of the steps you must pass through. The first version
   offered all 82 pending steps and let ROI order them, producing an order you cannot follow
   (A2·e32 above A1·e31) — ascension steps are strictly sequential, and cumulative rows per
   milestone would double-count. Live: `A1 · e31 — 1x Crop Plot + 1x Stone Rock · přes Ascension
   A1`, 50.80 FLOWER, +1.10/day, 46 d.
3. **Type filters on the table** (asked for separately). Five kinds now, each button with its own
   count, persisted in localStorage. Hidden types leave the RENDER only — order, Σ Cost and ETAs
   still come from the full list, and row numbers keep their true positions so it is visible that
   something was skipped. The badge also knew only four types and labelled an expansion "C".
4. **Compost tree no longer values 0**, and the two causes are kept apart: output skills
   ("+5 Sprout Mix") are priced through the composter model; the three POWER skills ("on all
   plots") report a per-ACTIVATION figure and deliberately stay out of `boostValues`, because a
   guessed daily rate beside measured ones would corrupt that column. Swift Decomposer comes out
   NEGATIVE — every composter nets negative here, so faster batches lose faster. Separate bug
   found: `fertiliserValue` ignored the `skillMultiplier` field the data always carried, so anyone
   with Fruitful Bounty had every Fruitful Blend valued at half.
5. **Roadmap renders once, not twice.** `roadmapItemCost` reads `PRICES()`, which returned null
   until its fetch resolved, so the page rendered price-less and the unconditional arrival
   rerender called `main()` — re-running section=power AND section=roadmap. Measured: 2 renders
   5.4 s apart, `power, roadmap, prices, ascension, power, roadmap`. Now awaits the price map after
   `buildPowerState` (earliest point the cache key exists): 1 render,
   `power, prices, roadmap, ascension`. The arrival rerender is also gated on a render having
   actually consumed a null map.
6. **`roadmapNodeCandidates` no longer guesses purchase counts** — it read
   `tiers.effective − BASE_NODE_COUNTS[island]` and called that "bought". Now
   `farmActivity["<Node> Bought"]`, the same input nodeAcq uses. The two rules give identical
   prices on 155498, so no visible number changed; the activity keys are the GAME's node names
   ("Gold Rock Bought", not "Gold Bought").
7. **Digging** is answered from `desert.digging.grid` — every hole with the items it yielded and
   the tool used, i.e. a measured sample rather than a treasure-probability model. Coin income and
   market income are kept apart because treasures sell for COINS and this app treats coins as free
   above 10k. On the live farm digging LOSES money: 1556 coins (≈1.47 FLOWER) against 2.57 FLOWER
   of shovels — −0.031/hole with coins counted, −0.073 without.

## Still open

- **Digging has no UI.** The engine, tests and `section=power → digging` are done and deployed;
  nothing renders it yet. It does not belong in the "CO VYPRŠELO" section (the owner cut that back
  to shrines + weather deliberately), so it needs a home — the POWER page's activity list is the
  natural one.
- **`roadmapNodeCandidates` still has its own MARGIN basis** (`gameResBoostedBase × cycles −
  toolPerNode`) where nodeAcq uses `perNodeSfl` from the power categories. The escalation is now
  shared; the valuation is not. Unifying it means the roadmap engine reading nodeAcq, which is
  circular today (ascension.mjs imports from roadmap.mjs).
- **Composting Overhaul / Revamp** ("±5 fertilisers") are still unvalued: it is not clear from the
  buff text WHICH fertiliser each affects, and guessing would be worse than the gap.

## Facts worth not re-deriving (additions)

- `desert.digging.grid` is one site's holes with `{dugAt, x, y, items, tool}` — a real per-hole
  sample. 35 holes on the live farm, 17 on the fixture. `streak.count` / `totalClaimed` also there.
- Salt Dino Egg has no NPC sell price and no p2p quote.
- `farmActivity` node keys are the game's node NAMES: "Gold Rock Bought", "Stone Rock Bought",
  "Tree Bought", "Crop Plot Bought", "Flower Bed Bought". No "Oil Reserve Bought" until you buy one.
- A `const` used before its declaration in the same scope throws on the temporal dead zone and
  kills the whole render — it cost a blank roadmap page once this session. Declare render tables
  above the code that reads them.
- UPPER_SNAKE object literals in flowers.html are read by the inventory scan as GAME DATA tables.
  A UI/render constant must be lowerCamelCase (`rowTypeMeta`, not `ROW_TYPES`).
- `gh` drifts back to the SoftimDevOps account and push 403s; `gh auth switch --user hlavasim`.

---

## Status update 3 — remaining items closed (through commit 9749dfa+)

- **Digging + composter UI.** One panel at the top of the POWER page, "VYPLATÍ SE? — kopání a
  composterty". Both were served and unrendered. It went in wrong the first time: the call sat
  between the debug table's last row and its `</tbody>`, so the parser dropped the whole panel —
  the function ran and returned 4.4 kB that never reached the DOM. Verified in the browser
  (`typeof` function, length 4441, panel absent) before moving it.
  **On the LIVE farm Compost Bin is POSITIVE (+0.062/day)** while Turbo (−0.130) and Premium
  (−0.310) lose — the fixture, where all three lose, is not representative.
- **A skill point you hold is NOT free.** Owner's correction, and the analogy is the argument: the
  buy path prices an NFT at its floor whatever your FLOWER balance is. Zeroing the cost also broke
  the ordering it was meant to help — payback 0 put eight +0.03/day skills above every real
  purchase. Now always `points × sflPerPoint`, with "MŮŽEŠ HNED" as a badge that cannot touch the
  price. Top of the path became A1·e31 (45 d) → Speed Miner L2 (227 d) → Oil Be Back L2 (259 d).
- **Node income in the buy path now comes from nodeAcq.** The local basis disagreed by 0.19x–3.33x
  (Tree 0.43/day vs 2.29, Crimstone 1.13 vs 0.34). Without nodeAcq there are NO node rows, by
  choice — falling back is what caused the disagreement.
- **Composting Revamp is modelled**, and the "±5 fertilisers" question is answered from
  `getCompostAmount`: the three `+N` skills are each gated to ONE building, Revamp has no building
  check (so it is worth whichever composter you run — never the sum of all three), and
  **Composting Overhaul does not appear in that function at all**, so its "-5 fertilisers" debuff
  text is not something the game applies to the produce count. No penalty is modelled for it.

## Still open

- Nothing from the original list. Two smaller things noticed along the way:
  - the "CO VYPRŠELO" section renders empty whenever nothing has lapsed, which is correct but means
    the roadmap page silently has no such section most of the time;
  - `perBatch` on the Composting Revamp row is derived by dividing the daily value back down, which
    is fine but is the only figure in that engine not computed forward from a batch.
