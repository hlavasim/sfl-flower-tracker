# Handoff — 2026-07-29

State at the end of a long session. Everything listed as done is deployed and verified on
production; everything under "Open" is diagnosed but not started.

> **Status update, same day (commit 84cda7a).** All five open items below were taken on and
> deployed. Two corrections to what this document claims:
>
> - **§2 is wrong.** The cause was neither the `cost_reduction` branch nor a feed-savings
>   modelling gap. Measured on the fixture farm, *every* chicken boost read 0 — including
>   Rich Chicken's plain `+0.1 Egg`. Two real causes: `calcBoostValue` clamped **each side**
>   of its before/after difference at zero, which zeroes every boost on a loss-making
>   category (fixture chickens net −0.87/day); and `free_feed` was an inert effect,
>   `calcAnimalFeedCost` only ever consulted `capacity.goldenAnimals`. Both fixed and pinned.
> - **§2's example is invisible on the live farm**, which currently has **0 chickens** —
>   so every chicken boost is legitimately 0 there. Use the fixture to see the fix.
>
> §1 (bud ROI), §3 (`—` vs `?`) and §5 (Azure publish, done — 11 functions live) are
> complete.
>
> - **§4 is also wrong: the scroll jump was already fixed** before this session, by the
>   two-frame `_worldRestoreY` restore in `_fetchWorld` — whose own code comment says
>   "held with this". Measured on production with Playwright driving Edge against the live
>   page: a scope change holds the offset at 300 / 1000 / 1800 / 2000 / 3000, on every
>   transition, and still holds under 6× CPU throttling with 400 ms / 400 kbps network. It
>   holds even with `_worldKeepScroll` replaced by a no-op at runtime (the override was
>   verified to reach the call site), so nothing in that function is carrying it.
>
>   A min-height pin was written for §4 and then **reverted** once measured — it changed no
>   observed behaviour, and its rationale ("none of the earlier attempts held") was false.
>   If a jump is ever seen again, measure before changing anything: `_worldKeepScroll` is
>   not where the fix lives.
>
> Two more bugs surfaced while answering questions about the above, both older than this
> session and both fixed:
>
> - **Dev Wrench was valued at −0.47 instead of +11.42 FLOWER/day.** Its boost text is
>   `-50% Oil Regeneration Time`, but the oil speed rule only accepted
>   refill/recovery/respawn — and `regeneration` is the ONLY wording sfl.world uses after
>   "Oil … time", so that rule matched nothing at all in the live feed. The text fell through
>   to the generic `N% <Product>` rule and became `yield_pct -50`, i.e. HALF the oil, from a
>   boost that halves the timer. (Oil is genuinely non-sellable — `unitToSfl` returns 0 — but
>   it is in `ROADMAP_MINING_CATS`, so the sell-vs-use chain credits what it enables.)
> - **`section=wishlist` 500'd on a cold instance:** `Cannot read properties of null (reading
>   'boostItems')`. It called `roadmapComputeEfficiency` before `buildPowerSection`, but that
>   function reads `powerState`. Every other section builds the context first. It hid because
>   the `rows.length < 2` guard returns before the first dereference, and every test and probe
>   posted `snapshots: []` — only a real page, which posts its history, reached it. On a warm
>   instance it did not throw at all; `powerState` is module-level, so it measured one farm
>   against whichever farm the instance had served last.
>
> **Drift between `core/` and `flowers.html` is now a test** (`tests/core/twin-copies.test.mjs`):
> 100 shared functions pinned identical, 17 deliberate deviations allow-listed with reasons,
> checked in both directions. Patching one copy and forgetting the other now fails the suite
> instead of needing to be caught by eye.

## Conventions this repo expects

- `flowers.html` and `index.html` are byte-identical twins. `index.html` is what production
  serves. Deploy = `cp flowers.html index.html`, commit, `git push` (main auto-deploys on
  Vercel). Azure Functions do **not** deploy on push — they need
  `func azure functionapp publish`.
- Run `npm test` before every deploy. Current: **219/219**.
- A test that pins a bug must be shown to FAIL when the fix is reverted. Several bugs this
  session were only caught that way; a green suite proved nothing on its own. When you revert
  a fix to check that, **verify the revert actually applied** — a scripted edit that silently
  no-ops (wrong line endings, say) leaves the suite green and reads as proof.
- A fix you cannot measure is not a fix. `playwright-core` driving installed Edge
  (`chromium.launch({ channel: "msedge" })`) is enough to measure the deployed page — no
  browser download, and it can override page functions at runtime for an A/B. §4 below was
  shipped on a stale bug report and had to be reverted for want of exactly this.
- Sections that use the roadmap engine must call `buildPowerSection` FIRST: the engine reads a
  module-level `powerState`, which also survives between invocations on a warm instance, so
  getting the order wrong fails loudly when cold and silently mixes farms when warm.
- `core/` is a port of functions that also exist inline in `flowers.html`. **Both copies must
  be patched.** This bit twice today (see `unitToSfl`).
- `tests/core/constants.test.mjs` reads UPPER_SNAKE object/array literals in `flowers.html`
  as game-data tables and fails unless they are registered in `core/data/_inventory.mjs`.
  UI/render constants must therefore be lowerCamelCase (`nodeIconUrl`, `buyableNodes`).

## Open work, highest confidence first

### 1. Wishlist ROI for buds (and pets, which are counted within buds)

Real wiring bug, cause confirmed. `flowers.html` pushes bud rows onto `d.rows` **after** the
server response:

```js
d.rows.push({ key, name: bc.label, floor: bc.floor, ... })   // keys like "buds:<id>"
```

`buildWishlistSection` (`core/sections/wishlist.mjs`) only knows `collectibles` and
`wearables`, so it never sees them and `perDay` / `perDayEff` / `roiDays` / `roiDaysEff` are
undefined for every bud. The ROI column is not empty for buds, it is unreachable.

Fix direction: value bud boosts with `core/engine/buds.mjs` (exists) and put buds through the
same path as other rows — preferably by resolving `buds:` server-side so they stop being an
exception. Pets ride along, per the owner: "pets jsou počítány v buds".

### 2. Chicken / feed boosts value to zero

Not a modelling gap — I first reported that and was wrong (I looked up "Golden Egg"; the item
is **"Gold Egg"**). They are present and computed:

```
Gold Egg        chickens  synergy 0.000
Speed Chicken   chickens  synergy 0.000
Eggplant Onesie crops     synergy 0.000
```

`calcBoostValue` (`core/engine/roadmap.mjs:388`) has a `cost_reduction` branch, but it prices
savings on **tools** (`coinSavingsPerUse * baseCost.toolsPerDay`). Animal-feed savings do not
fit it and fall through to 0. Verify how feed savings parse into effects in
`core/engine/power-boosts.mjs` before changing anything — do not guess.

### 3. Wishlist column should distinguish "—" from "?"

Today a value of zero and a value we cannot compute both render `—`, which reads as
"worthless". Split them once #2 is understood.

### 4. WORLD activity filter jumps to the top of the page

Not fixed after three attempts, all deployed and measured. Node-type switching **is** fixed
(scroll 2186 → 2186, page rebuilt 0×, never blank) because it repaints only
`#world-node-panel`. The scope switch still measures 300 → 0.

Tried and insufficient: restoring scroll around each paint (`_worldKeepScroll`), forcing
layout with `body.offsetHeight` first, and capturing the offset on click to reapply after the
final paint over three frames. The scroller is `document.documentElement` — that was verified,
so the measurement is not the problem.

Likely direction: stop replacing `#app` wholesale on a scope change and repaint the individual
chart bodies, the way the node panel now does.

### 5. Azure function not published

`azure-functions/shared/world-warm.js` + the call in `world-cdn/index.js` are committed but
**not deployed**. Until `func azure functionapp publish` runs, the World chart cache is not
warmed after the daily ingest, so the first visitor after a new dump pays ~19 s per chart
(then it is cached). Needs the owner's go-ahead.

## Done this session (deployed, verified)

- **Non-sellable resources are not income.** Obsidian and oil cannot be sold
  (`TradeResource` in the game's `actions/tradeLimits.ts` excludes them; `SELLABLE` in
  `sellCrop.ts` is crops + fruit only). sfl.world publishes an Obsidian price (~20 FLOWER)
  and the code took it at face value: **103.3 FLOWER/day of unrealisable income, 36.8 % of a
  real farm's reported total**. Fixed in `unitToSfl` — core *and* the `flowers.html` copy.
  Live: 281.7 → 164.3 FLOWER/day. A test pins the two copies together.
- **Lava Pit** added to `NODE_PRICES` (it was missing, so the advisor never offered the one
  node that makes obsidian) and to the expand-vs-buy table, with its return in obsidian/day
  rather than a fictional FLOWER figure.
- **Ascension step ROI** now uses net, efficiency-adjusted income. On the live farm A1·e33
  went 14 → 34 days, A1·e39 61 → 195.
- **Pet boost consistency**: banked food was valued with the ×1.5 pet streak while the XP rate
  was not, inflating every level ETA (16.4 h vs the correct 10.9 h). Forced on in
  `compute.mjs` for the ascension section.
- **Bumpkin recipe selection** is now sent to `section=ascension`; without it the server rated
  all six kitchens and reported 930,913 XP/day instead of the farm's real 677,328.
- **World node-distribution chart** (`farm_world.node_tiers`, backfilled for all 656,470
  farms; `mode=nodes`), stacked into only-t1 / has-t2 / has-t3.
- **`world_agg`**: per-generation cache of each chart's response, computed on miss.
  `mode=nodes&node=trees` 26.0 s → **0.21 s**; whole table 43 kB.
- **Farm fetch cached in KV for 20 s** (`api/proxy.js`): a page load was rate-limiting itself
  into 502s. Load ~9 s with two 502s → 2.7–4.7 s, none.
- **Wishlist FLOWER/day + ROI** with a theoretical / measured-efficiency toggle, fed by POSTed
  farm-history snapshots.
- **WORLD shows the data's date** and age from `dump.generated_at`.

## Facts worth not re-deriving

- Merging exists only for trees, stones, iron, gold (`multiplier` 4 then 16); verified across
  the whole active population that no other type has `multiplier > 1`. ~2 % of farms have any
  merge; exactly one farm has a T3 gold rock.
- 10 buyable node types with prices come from the game's `RESOURCE_NODE_PRICES`
  (`buyResource.ts`); Lava Pit is 40 / +40, volcano-gated.
- Ascension levels: `totalBumpkinLevel(xp, a) = 150 + (a-1)*50 + withinAscensionLevel(xp, a)`,
  all read off one running XP total. Band A1 costs 50 M XP for 50 levels, A2 75 M (×1.45 each
  band). Nothing resets on ascending — the same XP simply reads as more skill points because
  band levels are cheaper than the 151–200 table stretch. On the owner's farm: 186 unascended,
  200 at A1, 228 at A2 (239 counting banked food).
- Ascension is gated behind `SWAMP_ASCENSION` (beta). The owner expects it to go live in ~4
  days (so around **2026-08-02**). When it does, `getMaxBumpkinLevel` flips 200 → 150 and our
  uncapped pre-ascension `total_level` will overstate non-ascended farms against the game —
  revisit `total_level` semantics and the WORLD skill-point chart then.
- The community API serves the last **saved** farm state; a collect showed up within ≤5.2 min.
  Our own caches were ruled out (proxy does not cache farm data beyond the new 20 s TTL,
  Cloudflare `DYNAMIC`, compute TTL 10 s).
